import {defineCliConfig} from 'sanity/cli'

export default defineCliConfig({
  api: {
    projectId: 'z3dq9mvc',   // <- fill this in
    dataset: 'production',          // or your dataset name
  },
})